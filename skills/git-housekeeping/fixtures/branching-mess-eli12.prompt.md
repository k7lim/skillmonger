Explain the branching mess in this repo ELI12 and figure out what needs deciding. Plan only: inspect with read-only git (status, log, branch -vv, diff) and describe the commit trail you would build for this tree, but do not run any git command that writes — no add, commit, stash, checkout, rebase, pull, or push. End with a report that covers the commits you would make, the working-tree status they would leave, the remote/upstream state, what verification (tests, validate script) you would run before landing, and whatever would be left uncommitted or untracked.
