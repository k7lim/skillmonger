Explain how an HTTP request works visually, end to end, as a town the request drives through: DNS lookup, TCP and TLS handshake, load balancer, router, middleware chain (auth, rate limit), handler, database query, response serialization, CDN cache on the way back. Simulate a real request object with headers and a status code that changes at each stop, with station-by-station narration. Plain HTML plus canvas 2D and JS, written to ~/scratch/iso-http-request/.
