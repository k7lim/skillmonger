Make a TokenTown-style explorable for a PostgreSQL write path: client sends INSERT, parser, planner, executor, buffer pool page lookup, WAL append, commit and fsync, background writer, checkpoint to the heap file on disk. A row-sized vehicle should carry the actual tuple and its LSN through each station so the reader sees why the WAL is written before the data page. One self-contained static site under ~/scratch/iso-pg-write/.
